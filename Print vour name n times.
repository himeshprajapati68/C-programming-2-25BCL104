#include <stdio.h>

int main() {
    int i, n;
    char name[] = "Himesh";  // Replace "Himesh" with your name

    printf("Enter the number of times to print your name: ");
    scanf("%d", &n);

    for(i = 1; i <= n; i++) {
        printf("%s\n", name);
    }

    return 0;
}
