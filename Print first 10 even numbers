#include <stdio.h>

int main() {
    int i;

    printf("The first 10 even numbers are:\n");
    
    for(i = 2; i <= 20; i += 2) {  // Increment by 2 to get even numbers
        printf("%d ", i);
    }

    return 0;
}
