#include <stdio.h>

int main() {
    int i;

    printf("The first 10 odd numbers are:\n");
    
    for(i = 1; i <= 20; i += 2) {  // Increment by 2 to get odd numbers
        printf("%d ", i);
    }

    return 0;
}
